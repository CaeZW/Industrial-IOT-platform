# Development

Development is local and on-premise.

Rules:

- work only against local Docker services unless explicitly performing controlled integration;
- do not use production services for normal development;
- keep `.env` uncommitted;
- prefer small vertical changes;
- run relevant checks after application changes.

Current implementation plan: `sprint-1.md`.
